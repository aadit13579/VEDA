Veda Backend has all the Backend structure.
Veda Backend has 1.Routers-> All Api's (Application Program Interface) for fetching Data from backend and processing it.
Redis-http://localhost:8081
uvicorn main:app --reload

Main commands :
cd VEDA_backend
docker compose up -d
uvicorn main:app --reload